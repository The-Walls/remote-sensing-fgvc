# 遥感舰船细粒度识别：FGSCR-42 的泄漏审计与分组感知评测 — 实验报告

> 版本 v2（2026-09-05）。全部数字来自 commit `bc73f71`（`L2r` 为 `e7dc49b`）的 3 种子重跑：分组感知
> train/val/test = 60/20/20，val 选 epoch，**test 在该 epoch 上报分**。完整表见
> `runs/_analysis/TABLE.md`（`tools/analyze.py` 生成）。第一轮结果（seed 0、仅用 val、
> 旋转填充修正前）归档在 `runs/_v1_seed0_valonly/`，与本版结论的差异见附录 A。

---

## 0. 结论速览

1. **FGSCR-42 有严重的近重复问题。** 7778 张图中 3703 张（47.6%）属于某个近重复组，
   去重后只剩 **5233** 张唯一图像（塌缩 32.7%）。朴素分层切分让 **39.78%** 的 held-out
   图像在训练集里有孪生。同一种子、同一模型下，朴素切分比分组感知切分的 test
   overall top-1 高 **+1.22 ± 0.18** 个百分点、support≥5 的 mean-per-class 高
   **+3.42 ± 1.41** 点（§4.3）。
2. **用 val 选 epoch 又在 val 上报分，会系统性地高报。** 11 档中 9 档的 val@best 比
   test@best 高 0.9–5.3 点 mean-per-class，跨档平均 **+2.0** 点（§4.4）。这是第一轮
   报告中大部分"模块收益"的来源。
3. **在干净协议下，大多数模块差异落在种子噪声之内**（mean-per-class 的种子间标准差
   1–2.7 点）。能站住的只有：紧凑双线性池化**有害**（top-1 −2.13 ± 0.78）；
   `rs_rot` 里的"放大再中心裁剪"**单独看是有害的**（top-1 −1.48 ± 0.22），朝向增广
   要在它之上才换回 **+2.06 ± 0.35** 点 support≥5 MPC（三个种子全正，§4.6）。
4. **去掉缩放后的朝向增广是全阶梯最有效的单项改动。** 用反射填充代替放大裁剪
   （`L2r`），ResNet50 相对不加增广的 `L1_448` 提升 top-1 **+0.88 ± 0.11**、support≥5
   MPC **+2.51 ± 0.79**（三个种子全正），达到 top-1 **99.29 ± 0.56**、support≥5 MPC
   **99.28 ± 0.39**，与换成 ConvNeXt-Tiny（99.09 ± 0.33）持平，且不增加任何参数或计算。
5. **尾部类主导 mean-per-class。** 三个 test 集里各有 12–14 个类只有 1–4 张图，
   `Horizon-class`（1 张）、`Zumwalt-class`（2 张）、`Mistral-class`（1 张）几乎在所有
   模型上全错，把全类 MPC 的上限压到约 93–95%。因此本报告以 **support≥5 的 MPC**
   和 overall top-1 为主指标，全类 MPC 只作参考。

---

## 1. 数据集审计

### 1.1 FGSCR-42

来源：[DYH666/FGSCR-42](https://github.com/DYH666/FGSCR-42)（百度网盘发布）。
公开发布版实际到手 **7778** 张，少于论文标称的 ~9320 张；**本报告的一切数字都基于
这个 7778 张的版本。** 原始格式为未压缩 BMP，共 8.4 GB；已用 `tools/make_cache.py`
转为短边 ≤512 的 JPEG 缓存（663 MB），512 高于所有分辨率档位，实验不会看到被降质的
图像。解压包内的 macOS 残留（`__MACOSX` 下 6275 个 `._*` 文件、23 个 `.DS_Store`）已删除。

| 指标 | 值 |
|---|---:|
| 类别数 | 42 |
| 图像数（原始 / 去重后唯一） | 7778 / **5233** |
| 每类中位数 | 78 |
| 最多类 `029.Towing_vessel` | 778 |
| 最少类 `037.Horizon-class_destroyer` | **2** |
| 长尾比（原始 / 训练集实际） | 389x / **472x** |
| < 30 张的类 | 14 |
| 短边 p05 / p50 / p95 | 64 / 512 / 1112 |
| 短边 < 224 的图像 | 1884（24.2%） |
| 短边 < 448 的图像 | 3404（43.8%） |

图：`runs/_data/FGSCR-42/distribution.png`、`dup_examples.png`、`dup_groups.png`、
`shared_background.png`。

**分辨率扫描在本数据集上是有效实验。** 短边中位数 512、p95 1112，224→320→448
每一档都在读取真实像素而非插值；"24.2% 的图像短边不足 224"本身即是领域难点 1
（目标像素少）的量化证据。

### 1.2 近重复检测与泄漏

`tools/dedup_check.py`：aHash + pHash 双 64 位哈希，对每张图同时计算 rot90/180/270
与水平/垂直翻转共 6 个变体，在全图和中心 60% 裁剪两个尺度上比较；两哈希汉明距离都
≤5 判为重复，并查集合并成组。

| 项目 | 值 |
|---|---:|
| 重复对 | 4838 |
| 成员 >1 的组 / 最大组 | 1158 / 9 张 |
| 跨类重复对（同一张图两个标签） | 2 |
| 共享背景板但目标不同的对 / 其中跨类 | 375 / 10 |
| 朴素切分下 held-out 中有训练孪生的图像 | **1240 / 3117 = 39.78%** |

"共享背景板"一项说明部分图像是合成的（同一块海面贴不同舰种），这类对**不**并入重复组，
只单独计数。

**反向验证：分组感知切分自己泄漏吗。** 分组是在汉明 ≤5 上建的，该阈值下泄漏为 0
是构造使然。放宽阈值重测，问的是严格阈值漏掉了多少近重复：

| 汉明距离 ≤ | train/held-out 对 | held-out 中有训练孪生 | 占 held-out | 其中跨类 |
|---:|---:|---:|---:|---:|
| 5 | 0 | 0 | 0.00% | 0 |
| 8 | 93 | 53 | 1.74% | 2 |
| 10 | 150 | 98 | 3.22% | 6 |
| 12 | 288 | 199 | 6.55% | 33 |
| 16 | 1673 | 838 | 27.57% | 941 |

"跨类"是阈值失效的刻度：阈值 16 上 941/1673 对把不同舰种判成重复，该行是上界而非
测量。可信区间在 8–12 之间，**残余泄漏约 1.7%–6.6%**。本报告的数字不是泄漏刷出来的，
但也不是绝对干净；pHash 只抓像素级近重复，语义近重复（同一艘船不同时相）需要
embedding 相似度另测，列为局限（§6）。

### 1.3 MTARSI-fixed（框架验证 / 数据质量审计）

来源：[amistele/MTARSI-fixed](https://huggingface.co/datasets/amistele/MTARSI-fixed)，
原始 MTARSI 见 [Zenodo](https://zenodo.org/records/3464319)。2264 张 / 27 类。
该数据集不适合承担本课题的核心结论：短边 p95 = 286，100% 的图短边 < 448，@448 只是
上采样；27 类中 20 类恰好 100 张，长尾是截断出来的（4.3x）；检出 63 对"共享背景板但
目标不同"的合成图像（6 对跨类）。另检出真重复 226 对 / 371 张（16.4%）。
图：`runs/_data/MTARSI-fixed/dup_crossclass.png`。

---

## 2. 评测协议

| 要素 | 做法 | 理由 |
|---|---|---|
| 切分 | 按类别分层，把**唯一图像组**的 60/20/20 分到 train/val/test，整组不跨集 | 近重复不跨集（§1.2）；只有 2 个组的类分成 test 1 / train 1 / val 0，只有 1 个组的类只进 train |
| 选 epoch | val 的全类 mean-per-class top-1 最高的 epoch | 与第一轮一致，便于对照 |
| 报分 | **test 在该 epoch 上的指标**，每个 epoch 都算 test 但只在选定 epoch 读数 | 在选 epoch 的同一个集合上报分是取噪声序列的最大值（§4.4 实测） |
| 种子 | 0 / 1 / 2，种子同时控制切分与初始化 | `±` 含切分方差，是对"换一份数据"的稳健性估计 |
| 配对比较 | 同一种子 = 同一切分，模块间差分按种子配对后再取均值 ± 标准差 | 去掉切分方差，只看模块本身 |
| 指标 | overall top-1、全类 MPC、**support≥5 的 MPC**、macro-F1 | 472x 长尾下 overall 掩盖尾类；全类 MPC 被 1–2 张图的类主导（§4.8） |

三个种子的切分：train 4738 / 4634 / 4789，val 1558 / 1550 / 1512，test 1482 / 1594 / 1477；
每个种子有 1 个类在 val 里没有样本，test 里 42 类齐全。

---

## 3. 方法阶梯

| 模块 | 文件 | 对应领域难点 |
|---|---|---|
| 分辨率作为一等配置变量（224 档有 24.2% 的图短边不足、448 档 43.8%） | `configs/fgscr42/L1_*.yaml` | **难点 1** 目标像素少 |
| `rs_rot`：二面体群（h/v 翻转 + 90/180/270）+ ±30° 自由旋转，先放大 cos30+sin30=1.366x 再中心裁剪消除旋转黑角 | `src/transforms.py` | **难点 2** 朝向任意 |
| `rs_zoom`：只保留 `rs_rot` 的放大与中心裁剪，不做任何旋转翻转 | `src/transforms.py` | 对照：把"朝向"与"缩放裁剪"拆开 |
| `rs_rot_reflect`：按测试尺寸 resize 后用反射填充到同样的 1.366x 画布再旋转、中心裁剪；中心保持测试尺度，黑角由镜像内容填充 | `src/transforms.py` | **难点 2**，去掉训练/测试尺度失配 |
| `CompactBilinearPooling`：Tensor Sketch 近似二阶特征编码 | `src/models.py` | **难点 3** 背景强干扰 |
| `CBAM`：通道 + 空间注意力，池化前对判别区域重加权 | `src/models.py` | **难点 3** 背景强干扰 |
| `cb_ce`：类别平衡交叉熵（Cui et al. 2019），β=0.9999；先算 label-smoothed CE，再仅按真实标签权重加权样本 | `src/losses.py` | **难点 4** 长尾 |
| 增广感知去重 + 分组感知三分切分 | `tools/dedup_check.py`, `src/data.py` | **难点 2 + 4 的交叉后果**：数据集用旋转副本扩充稀有类，直接制造泄漏 |

每一档相对父级**只改一个字段**，`analyze.py` 自动打印该字段 diff：

| id | 父级 | 改动 | 验证假设 |
|---|---|---|---|
| L0 | — | ResNet50 @224，基础增广 | 地板线 |
| L0_naive_split | L0 | `group_aware_split: true→false` | **量化泄漏** |
| L1_320 / L1_448 | L0 | `img_size: 224→320/448` | 分辨率 |
| L2 | L1_448 | `aug: basic→rs_rot` | 朝向增广 |
| L2z | L1_448 | `aug: basic→rs_zoom` | 只缩放不旋转的对照 |
| L2r | L1_448 | `aug: basic→rs_rot_reflect` | 去掉缩放的朝向增广 |
| L3 | L2 | `head: gap→compact_bilinear` | 二阶特征 |
| L4 | L2 | `head: gap→cbam` | 弱监督定位 |
| L5 | L2 | `loss: ce→cb_ce` | 长尾重加权 |
| L6a / L6b | L2 | `model: resnet50→convnext_tiny / vit_base_patch16_384` | backbone |

训练配方（`configs/base.yaml`）：AdamW，lr 1e-4，分类头 10x，weight decay 0.05 且
**不施加于 norm/bias**，5 epoch warmup + cosine，60 epoch，batch 32，bf16 自动混合精度，
label smoothing 0.1，`cudnn.deterministic = True`。ViT 用 384 预训练权重，448 输入只需
384→448 的位置编码插值。

---

## 4. 结果

### 4.1 主表：test 在 val 选定 epoch 上的指标，3 种子 mean ± std

| id | top-1 | 全类 MPC | MPC (support≥5) | macro-F1 | 参数 (M) | s/epoch |
|---|---:|---:|---:|---:|---:|---:|
| L0 | 97.54 ± 0.15 | 87.43 ± 1.37 | 95.66 ± 0.92 | 86.93 ± 1.28 | 23.59 | 3.7 |
| L0_naive_split | 98.76 ± 0.13 | 90.22 ± 2.20 | 99.08 ± 0.49 | 89.58 ± 2.15 | 23.59 | 3.6 |
| L1_320 | 98.33 ± 0.50 | 87.60 ± 1.81 | 97.02 ± 2.16 | 87.40 ± 2.13 | 23.59 | 6.8 |
| L1_448 | 98.41 ± 0.51 | 89.79 ± 2.17 | 96.76 ± 1.09 | 89.26 ± 3.23 | 23.59 | 12.1 |
| L2 | 98.34 ± 0.43 | 90.62 ± 2.31 | 97.11 ± 1.38 | 90.22 ± 2.90 | 23.59 | 13.3 |
| L2z_zoom_only | 96.93 ± 0.61 | 87.59 ± 2.73 | 95.04 ± 1.63 | 87.06 ± 2.71 | 23.59 | 12.5 |
| L2r_rot_reflect | **99.29 ± 0.56** | 93.15 ± 0.76 | **99.28 ± 0.39** | 92.57 ± 0.79 | 23.59 | 14.3 |
| L3_compact_bilinear | 96.21 ± 0.41 | 87.87 ± 2.47 | 93.15 ± 0.13 | 85.94 ± 1.52 | 23.85 | 14.2 |
| L4_cbam | 97.78 ± 0.99 | 90.08 ± 2.36 | 96.31 ± 1.07 | 88.98 ± 3.47 | 24.12 | 13.2 |
| L5_cb_ce | 98.10 ± 0.69 | 91.81 ± 1.16 | 97.62 ± 1.46 | 91.07 ± 1.98 | 23.59 | 13.3 |
| L6a_convnext_tiny | 99.09 ± 0.33 | 92.12 ± 1.06 | 98.36 ± 0.82 | 92.03 ± 1.45 | 27.85 | 13.3 |
| L6b_vit_base_p16 | 98.31 ± 1.23 | **94.26 ± 0.77** | 98.58 ± 0.59 | **93.26 ± 0.57** | 86.28 | 22.9 |

### 4.2 配对差分：相对父级，同种子配对后 mean ± std（百分点）

| 比较 | top-1 | 全类 MPC | MPC (support≥5) | macro-F1 | 判定 |
|---|---:|---:|---:|---:|---|
| L0_naive_split − L0 | **+1.22 ± 0.18** | +2.79 ± 3.18 | **+3.42 ± 1.41** | +2.65 ± 3.20 | 泄漏抬分，稳定 |
| L1_320 − L0 | +0.79 ± 0.35 | +0.17 ± 0.44 | +1.36 ± 2.29 | +0.47 ± 0.94 | top-1 小幅收益 |
| L1_448 − L0 | +0.87 ± 0.36 | +2.37 ± 1.92 | +1.10 ± 1.19 | +2.33 ± 2.60 | top-1 小幅收益；MPC 未超噪声 |
| L2 − L1_448 | −0.07 ± 0.24 | +0.83 ± 0.86 | +0.35 ± 0.40 | +0.96 ± 1.00 | 整体 ≈ 0（见 §4.6 拆分） |
| L2z − L1_448 | **−1.48 ± 0.22** | −2.21 ± 2.47 | **−1.72 ± 0.75** | −2.19 ± 2.37 | 缩放裁剪本身有害，稳定 |
| L2r − L1_448 | **+0.88 ± 0.11** | +3.35 ± 1.67 | **+2.51 ± 0.79** | +3.31 ± 2.52 | 去缩放的朝向增广，稳定 |
| L2r − L2 | **+0.95 ± 0.36** | +2.52 ± 2.08 | +2.17 ± 1.14 | +2.35 ± 2.36 | 去耦本身的收益 |
| L3 − L2 | **−2.13 ± 0.78** | −2.75 ± 3.80 | **−3.96 ± 1.47** | −4.28 ± 3.65 | 明确有害 |
| L4 − L2 | −0.57 ± 0.56 | −0.55 ± 0.48 | −0.80 ± 0.74 | −1.24 ± 0.68 | 无收益，略负 |
| L5 − L2 | −0.25 ± 0.56 | +1.19 ± 1.20 | +0.51 ± 2.72 | +0.85 ± 0.93 | 弱信号，未超噪声 |
| L6a − L2 | **+0.75 ± 0.46** | +1.50 ± 3.12 | +1.26 ± 1.49 | +1.81 ± 3.97 | top-1 收益；MPC 噪声内 |
| L6b − L2 | −0.03 ± 1.21 | +3.63 ± 2.29 | +1.48 ± 1.51 | +3.04 ± 2.87 | MPC 收益；top-1 不稳 |

粗体为差分绝对值超过其种子间标准差 2 倍的项。

### 4.3 泄漏效应

朴素切分相对分组感知切分（同种子、同模型、同超参）：test top-1 +1.22 ± 0.18，
support≥5 MPC +3.42 ± 1.41，三个种子全正。错分数从 35/38/39 张降到 21/17/20 张——
**近一半的"错误"在泄漏协议下被记忆掉了。** 泄漏对有充分支撑的类别影响最大，因为
重复组集中在样本多的类（稀有类的重复副本反而少）。

### 4.4 选择偏差：val@best 与 test@best

| id | best epoch（3 种子） | val MPC @best | test MPC @best | 高报 |
|---|---|---:|---:|---:|
| L0 | 24/14/56 | 90.39 | 87.43 | +2.96 |
| L0_naive_split | 29/36/57 | 92.74 | 90.22 | +2.53 |
| L1_320 | 38/24/53 | 92.87 | 87.60 | **+5.27** |
| L1_448 | 17/25/16 | 92.21 | 89.79 | +2.42 |
| L2 | 39/54/37 | 90.18 | 90.62 | −0.44 |
| L2z_zoom_only | 28/25/42 | 89.30 | 87.59 | +1.71 |
| L3_compact_bilinear | 45/50/36 | 87.29 | 87.87 | −0.58 |
| L4_cbam | 41/37/11 | 91.23 | 90.08 | +1.15 |
| L5_cb_ce | 57/45/42 | 92.69 | 91.81 | +0.88 |
| L6a_convnext_tiny | 30/31/47 | 95.55 | 92.12 | +3.43 |
| L6b_vit_base_p16 | 29/47/38 | 96.76 | 94.26 | +2.50 |

跨档平均高报 **+2.0** 点全类 MPC；overall top-1 的高报在 ±0.7 内。这就是"用 val
选 epoch 又报 val"协议下的系统偏差。它不是常数——`L1_320` 高报 5.3 点而 `L2` 为负——
所以**不能靠事后减去一个平均值来修正**，只能靠独立测试集。

**收敛窗口核对**：test@best 与 test 最后 10 个 epoch 的均值相差都在 0.85 点以内
（`L2z` +1.15 除外），说明 val 选出的 epoch 能代表训练完的模型，报分不依赖某个尖峰。

### 4.5 分辨率

224→320 与 224→448 在 top-1 上各带来约 +0.8 点（±0.35），两者之间没有可分辨的差别
（98.33 vs 98.41）；MPC 的差异不超过噪声。448 的代价是每 epoch 时间从 3.7 s 到 12.1 s。
第一轮"448 明确优于 320"的结论不成立，两者都只是"比 224 好一点"。

### 4.6 朝向增广的拆分：旋转有效，但被它需要的缩放裁剪抵消

`rs_rot` 必须先放大 1.366x 再中心裁剪才能去掉旋转黑角，这同时引入了训练/测试的尺度
失配（测试不放大）。`L2z` 只保留这一步：

| 比较（同种子配对） | 全类 MPC | MPC (support≥5) | 逐种子 MPC≥5 |
|---|---:|---:|---|
| 只缩放裁剪（L2z − L1_448） | −2.21 ± 2.47 | **−1.72 ± 0.75** | −0.87 / −1.98 / −2.30 |
| 缩放之上加朝向增广（L2 − L2z） | +3.03 ± 1.67 | **+2.06 ± 0.35** | +1.66 / +2.22 / +2.31 |
| 完整 rs_rot（L2 − L1_448） | +0.83 ± 0.86 | +0.35 ± 0.40 | +0.79 / +0.24 / +0.01 |

三个种子上，缩放裁剪一律扣分、朝向增广一律加分，两者几乎抵消。**结论：朝向增广对
有充分支撑的类别有约 +2 点的稳定收益，当前实现把它花在了消除黑角的缩放上。**
去掉尺度失配是最直接的改进，预期能兑现这 +2 点。

**验证（`L2r`）。** 用反射填充代替放大裁剪，中心区域保持测试尺度，三个种子上：

| 比较（同种子配对） | top-1 | 全类 MPC | MPC (support≥5) | 逐种子 MPC≥5 |
|---|---:|---:|---:|---|
| L2r − L1_448（增广的净收益） | **+0.88 ± 0.11** | +3.35 ± 1.67 | **+2.51 ± 0.79** | +1.98 / +2.14 / +3.42 |
| L2r − L2（去耦本身） | **+0.95 ± 0.36** | +2.52 ± 2.08 | +2.17 ± 1.14 | +1.19 / +1.90 / +3.42 |
| L2r − L2z（朝向增广，无缩放） | **+2.36 ± 0.32** | +5.56 ± 2.97 | **+4.23 ± 1.44** | +2.85 / +4.12 / +5.73 |

预期兑现：support≥5 MPC 的净收益 +2.51 与上表拆出的 +2.06 一致。`L2r` 的 test top-1
99.29 ± 0.56、错分 6 / 6 / 20 张，val→test 高报仅 +0.30，收敛值 99.34 与 test@best 一致。
它是全阶梯上唯一一个既超过噪声、又不增加参数和计算的改动。

### 4.7 池化头、损失与主干

- **紧凑双线性（L3）**：top-1 −2.13 ± 0.78，support≥5 MPC −3.96 ± 1.47，错分数
  49–64 张，是全阶梯最差的档，且每 epoch 更慢。明确不保留。
- **CBAM（L4）**：所有指标略负（top-1 −0.57 ± 0.56），种子间波动最大（top-1 ±0.99）。
  无证据支持。
- **CB-CE（L5）**：全类 MPC +1.19 ± 1.20，top-1 −0.25 ± 0.56。方向符合预期但未超噪声；
  第一轮的"+2.50 点、站得住"是选择偏差的产物（该档 val 高报 0.88，L2 为 −0.44）。
- **ConvNeXt-Tiny（L6a）**：top-1 **99.09 ± 0.33**，错分 19/11/11 张，是 top-1 最高、
  也是三个种子最一致的档；MPC 收益 +1.50 ± 3.12 在噪声内。参数 27.85M，速度与 ResNet50 相当。
  但与 `L2r`（ResNet50 + 去耦增广）配对比较，top-1 −0.19 ± 0.75、support≥5 MPC
  −0.91 ± 0.56：换主干的收益不超过修好增广的收益。
- **ViT-B/16（L6b）**：全类 MPC **94.26 ± 0.77**、support≥5 MPC 98.58 ± 0.59、macro-F1
  93.26 ± 0.57 为全类 MPC 与 macro-F1 的最高，但 top-1 98.31 ± 1.23 —— seed 0 错 46 张而 seed 1/2 各错 15 张。
  它在尾类上最好，在头类上不稳，且参数 3.7 倍、每 epoch 1.7 倍时间。`L2r` 在
  support≥5 MPC 上比它高 0.69 ± 0.42，在全类 MPC 上低 1.11 ± 1.45。

### 4.8 尾类与误差计数

test 集每 1,500 张左右的错分数（3 种子）：

| L0 | naive | L1_320 | L1_448 | L2 | L2z | L2r | L3 | L4 | L5 | L6a | L6b |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 35/38/39 | 21/17/20 | 19/24/33 | 21/19/32 | 26/19/30 | 46/39/54 | 6/6/20 | 60/64/49 | 37/18/45 | 39/20/27 | 19/11/11 | 46/15/15 |

三个 test 集里各有 12–14 个类只有 1–4 张图。`037.Horizon-class_destroyer`（1 张）、
`035.Zumwalt-class_destroyer`（2 张）、`039.Mistral-class_amphibious`（1 张）在几乎所有
模型、所有种子上全错；一张图对错就是全类 MPC 的 1/42 = 2.38 点。这解释了为什么全类
MPC 的种子标准差高达 1–2.7 点、为什么 ViT 在全类 MPC 上领先却在 top-1 上不稳。
头部类里稳定出错的是 `Ticonderoga` / `Arleigh Burke`（两者互混，各 110+ 张，准确率
88–97%）和 `San Antonio`。

---

## 5. 讨论

**能下的结论。**

1. FGSCR-42 的近重复泄漏是真实且量级可观的：朴素切分抬高 top-1 约 1.2 点、
   support≥5 MPC 约 3.4 点，并把错分数削去近一半。
2. 只有 val 的协议高报全类 MPC 平均 2 点、最多 5 点，且不是常数偏移。
3. 在干净协议下，模块阶梯只剩三条超出噪声的结论：紧凑双线性有害；朝向增广对有
   充分支撑的类别 +2.5 点，前提是去掉它原实现里的缩放裁剪（`L2r`）；换 ConvNeXt-Tiny
   的收益不超过修好增广的收益。
4. 更强 backbone（ViT）和类别平衡损失的收益集中在尾类，量级等于一两张图，
   3 个种子下不能与噪声区分。

**对已发表结果的含义。** 数据集没有官方切分，已发表的 98.4%–99.0%（MA-Net、FGShipNet）
都在各自的随机切分上取得。本报告中同一个 ResNet50 基线在朴素切分下为 98.76 ± 0.13，
在分组感知切分下为 97.54 ± 0.15；已发表的数字落在前一个区间。这不能证明它们受泄漏
影响，但说明**在这个数据集上不做去重就无法解释 1–2 点的差异**，而这正是这些方法之间
的差距量级。

**贡献定位。** 本工作的贡献在数据与评测一侧：泄漏的量化、可复用的分组感知切分
（`runs/_data/FGSCR-42/dup_groups.json`）、以及把"val 选 epoch 又报 val"的选择偏差
测成数字。模块消融是这些方法学结论的载体，不是新方法。此外有一条可直接复用的实现层
结论：遥感旋转增广不能用放大裁剪去黑角，要用反射填充；这一步值 top-1 约 1 点、
support≥5 MPC 约 2.5 点。

---

## 6. 局限

1. **单一数据集、单一版本。** 到手的是 7778 张的发布版，非论文的 9320 张；结论是否
   推广到其他遥感细粒度数据集（FGSC-23、HRSC2016 细粒度子集）未验证。
2. **去重只到像素级。** pHash 抓不到语义近重复；残余泄漏 1.7%–6.6% 是下界估计。
   补 embedding 相似度（如 DINOv2 余弦 > 0.95）的对照才能给出上界。
3. **test 约 1,500 张，尾类 1–2 张。** 全类 MPC 的分辨力受限，本报告因此以 support≥5
   MPC 与 top-1 为主指标。
4. **3 个种子。** 种子同时改变切分，标准差含切分方差；要把"模块效应"从"切分效应"
   里分得更干净，需要更多种子或固定切分只换初始化的补充实验。
5. **未复现已发表方法。** 要把 §5 中"已发表数字落在泄漏区间"从相关变成因果，需要
   在分组感知切分下复现至少一个公开实现。
6. **反射填充去耦只验证了 ±30° 一种角度范围。** 更大角度、以及镜像伪影对检测阶段
   的影响未测。

---

## 7. 复现

```bash
python tools/inspect_data.py  <root> --out runs/_data/FGSCR-42
python tools/dedup_check.py   <root> --out runs/_data/FGSCR-42 --val-ratio 0.2 --test-ratio 0.2
python tools/make_cache.py    <root> <cache> --max-short 512
python tools/run_ladder.py    configs/fgscr42/L*.yaml --seeds 0 1 2
python tools/analyze.py       --runs runs --baseline L0
python -m unittest discover -s tests -v
```

`run_ladder.py` 按种子优先排队，跳过已有 `metrics.json` 的任务，从 `latest.pt` 精确
恢复模型、优化器、学习率调度和随机状态。Windows 下用 `tools\launch_ladder.ps1` 以
隐藏控制台启动，避免关闭终端时整棵进程树被杀。3 种子全阶梯（12 档）在 RTX 5090 D 上约 10.5 小时
（每种子约 3.5 小时）。

环境：Python 3.14.3 / torch 2.11.0+cu128 / timm 1.0.27 / RTX 5090 D。
`cudnn.deterministic = True`；混合精度 bfloat16。

### 工程约束

- Windows 的 DataLoader 共享内存由分页文件支撑，8 workers × 448px 会触发
  `Couldn't open shared file mapping ... 1455`：`data.workers` 为 4，val/test 各取一半，
  任务串行排队。
- 训练一律读 JPEG 缓存而非原始 BMP，否则 60 epoch × 8.4 GB 的 I/O 会主导耗时。
- 每个 epoch 原子写入 `latest.pt`，最佳模型单独写入 `best.pt`；`metrics.json` 记录
  git commit。
- `cb_ce` 的实现陷阱：把 `weight` 与 `label_smoothing` 同时传给 PyTorch
  `CrossEntropyLoss` 会把稀有类权重施加到平滑目标的所有类别项。当前实现先算逐样本
  平滑 CE，再按真实标签权重加权（`tests/test_losses.py` 覆盖）。

---

## 附录 A：与第一轮（v1）结论的差异

| 结论 | v1（seed 0，val 选 epoch 并报分） | v2（3 种子，test 报分） |
|---|---|---|
| 泄漏效应 | overall −0.16 / MPC +1.96（一度读成"泄漏不抬 overall"） | top-1 +1.22 ± 0.18，MPC≥5 +3.42 ± 1.41，一致抬分 |
| 448 vs 320 | 448 明确优于 320 | 无可分辨差别 |
| rs_rot | +2.73 MPC，"最明确的模块" | 原实现整体 ≈ 0；拆开后朝向 +2.06 MPC≥5、缩放 −1.72；去耦后净收益 +2.51 |
| CB-CE | +2.50 MPC，"站得住" | +1.19 ± 1.20，噪声内 |
| L5/L6a/L6b | 并列 97.49，"阶梯分辨不出" | ResNet50 + 去耦增广（L2r）top-1 与 ConvNeXt 持平；ViT MPC 最优但 top-1 不稳 |
| 天花板 | "1880 张验证图只错 2–5 张" | 每 1,500 张 test 错 11–60 张 |

第一轮的"只错 2–5 张"是 44.78% 泄漏 + 选择偏差叠加的结果；两者去掉后，错误数回到
两位数，模块之间才重新有了可比的分辨力。

## 附录 B：第一轮实验中发现并修复的实现问题

1. `ROT_PAD = 1.15` 只覆盖 ±9.4°，66.5% 的 `rs_rot` 训练样本带黑角；现为
   cos30+sin30 = 1.366 并向上取整，`tests/test_transforms.py` 在 224/320/448 三个尺寸的
   极限角度上验证黑角为零。
2. weight decay 曾施加到 norm/bias 参数；现按 timm 配方排除。
3. `L6b` 曾用 224 预训练权重插值到 448；现用 384 权重。
4. OOM 回退分支曾不回卷学习率调度的 step 计数；已修复（本轮未触发 OOM）。
5. `upsampled` 标志用全库 p95 判定，恒假；已移除，分辨率覆盖率按图统计在 §1.1。
