# 遥感图像细粒度识别 — 实验报告

> ## ⚠️ 数据泄漏警告（置顶，按阈值触发）
>
> **FGSCR-42 的朴素分层切分会造成 44.78% 的验证集泄漏。**
>
> 全库 7778 张图中，**3703 张（47.6%）属于某个近重复组**；按 aHash+pHash 双哈希、
> 叠加 rot90/180/270 与水平/垂直翻转变体、全图与中心裁剪双尺度、汉明距离 ≤5 判定。
> 去重后唯一图像只剩 **5233** 张（塌缩 32.7%）。若不做分组感知切分，1943 张验证图中
> 有 **870 张**在训练集里存在孪生，验证指标会混入对近重复样本的记忆。
>
> 本报告所有结果均使用**分组感知切分**（同一重复组整组进同一 split）。
> `L0` 与 `L0_naive_split` 是专门用于量化该效应的对照，两者除
> `data.group_aware_split` 外配置完全一致。按收敛值（见 §4 的报数口径），朴素切分
> 相对 L0 是 overall **+0.48**、全类 MPC **+0.96**、support≥5 MPC **+1.68** 个
> 百分点——**泄漏把所有指标一致地抬高**，其中对有充分支撑的类别影响最大。
>
> 泄漏率由 `tools/dedup_check.py` 用 `src/data.py:split_indices`
> （`group_aware=False`）计算，即 `L0_naive_split` 实际训练的那个切分本身，
> 而不是另起一个随机切分去估计。

### 反向验证：分组感知切分自己泄漏吗

只证明"朴素切分会泄漏"是半个论证。分组是在汉明距离 ≤5 上建的，所以该阈值下泄漏为
0 是构造使然、不构成证据。放宽阈值重测才是真问题——有多少近重复是严格阈值漏掉的：

| 汉明距离 ≤ | 跨集合对数 | val 中有 train 孪生的图 | 占 val | 其中跨类 |
|---:|---:|---:|---:|---:|
| 5 | 0 | 0 | 0.00% | 0 |
| 8 | 93 | 51 | 2.71% | 2 |
| 10 | 150 | 79 | 4.20% | 6 |
| 12 | 288 | 148 | 7.87% | 32 |
| 16 | 1673 | 592 | 31.49% | 721 |

「跨类」是阈值失效的刻度：阈值 16 上 721/1673 对把不同舰种判成重复，该行是上界而
非测量值。可信区间在 8–12 之间，**残余泄漏 2.7%–7.9%**。结论：本报告的数字不是
泄漏刷出来的，但也不是绝对干净——尾部类只有 1–2 张验证图，几张残余孪生就能改变
mean-per-class 的小数点后一位。

---

## 1. 数据集审计

### 1.1 FGSCR-42（主数据集）

来源：[DYH666/FGSCR-42](https://github.com/DYH666/FGSCR-42)（百度网盘发布）。
公开发布版实际到手 **7778** 张，少于论文标称的 ~9320 张。
原始格式为未压缩 BMP，共 8.4 GB；已用 `tools/make_cache.py` 转为短边 ≤512 的 JPEG
缓存（663 MB，7.5%），512 高于所有分辨率扫描档位，实验不会看到被降质的图像。
解压包内的 macOS 残留（`__MACOSX` 下 6275 个 `._*` 文件、23 个 `.DS_Store`）已删除。

| 指标 | 值 |
|---|---:|
| 类别数 | 42 |
| 图像数（原始 / 去重后唯一） | 7778 / **5233** |
| 每类中位数 | 78 |
| 最多类 `029.Towing_vessel` | 778 |
| 最少类 `037.Horizon-class_destroyer` | **2** |
| 长尾比（原始 / 去重后 / 训练集实际） | 389x / 274x / **583x** |
| < 30 张的类 | 14 |
| 短边 p05 / p50 / p95 | 64 / 512 / 1112 |
| 短边 < 224 的图像 | 1884（24.2%） |
| 短边 < 448 的图像 | 3404（43.8%） |

图：`runs/_data/FGSCR-42/distribution.png`、`dup_examples.png`、`dup_groups.png`

**分辨率扫描在本数据集上是有效实验。** 短边中位数 512、p95 1112，224→320→448
每一档都在读取真实像素，而非插值。同时「24.2% 的图像短边不足 224」本身即是
领域难点 1（目标像素少）的量化证据。

**尾部类的统计约束。** 去重后 `037.Horizon-class_destroyer` 仅 2 张、
`035.Zumwalt-class_destroyer` 3 张，7 个类 < 10 张，14 个类 < 20 张。
处理方式：**保留全部 42 类**以保持与已发表结果的可比性，保证每类至少 1 张验证样本；
`metrics.json` 同时记录全 42 类的 mean-per-class 与**仅验证支持 ≥5 的类**的
mean-per-class（`mean_per_class_top1_support_ge5`），两者在对照表中并列，
使尾部噪声可被隔离而非被平均掉。

### 1.2 MTARSI-fixed（框架验证 / 数据质量审计）

来源：[amistele/MTARSI-fixed](https://huggingface.co/datasets/amistele/MTARSI-fixed)，
原始 MTARSI 见 [Zenodo](https://zenodo.org/records/3464319)。2264 张 / 27 类。

该数据集**不适合承担本课题的核心结论**，原因有三，均由体检得出：

1. **分辨率不足**：短边 p95 = 286，最大 360，100% 的图短边 < 448。
   在其上做 @448 实验只是上采样，不构成分辨率假设的检验。
2. **长尾是截断出来的**：27 类中 20 类恰好 100 张，说明在 100 处被人为砍平；
   真实尾部仅 4 类，长尾比 4.3x，属轻度不平衡，重加权方法在此量级增益通常落在噪声内。
3. **图像为合成**：检出 63 对「共享背景板但目标不同」的图像（100 张，4.4%），
   其中 6 对跨类——同一块停机坪上分别贴着 C-130 和 F-16。
   这意味着**领域难点 3（背景强干扰）在该数据集上不成立**，背景是复用的板子而非真实场景杂波。
   图：`runs/_data/MTARSI-fixed/dup_crossclass.png`

另检出真重复 226 对 / 371 张（16.4%），朴素切分泄漏率 13.81%。

---

## 2. 模块 ↔ 领域难点对应

| 模块 | 文件 | 对应领域难点 |
|---|---|---|
| 分辨率作为一等配置变量（224 档有 24.2% 的图短边不足、448 档 43.8%，见 §1.1） | `configs/*/L1_*.yaml`, `train.py` | **难点 1** 目标像素少 |
| `rs_rot` 增广：二面体群（h/v 翻转 + 90/180/270）+ ±30° 自由旋转，先放大 cos30+sin30=1.366x 再中心裁剪以消除旋转黑角 | `src/transforms.py` | **难点 2** 朝向任意 |
| `CompactBilinearPooling`：Tensor Sketch 近似二阶特征编码，替代会被背景稀释的全局平均池化 | `src/models.py` | **难点 3** 背景强干扰 |
| `CBAM`：通道 + 空间注意力，池化前对判别区域重加权（弱监督定位） | `src/models.py` | **难点 3** 背景强干扰 |
| `cb_ce`：类别平衡交叉熵（Cui et al. 2019），按有效样本数 `(1-β^n)/(1-β)` 重加权，β=0.9999；先计算 label-smoothed CE，再仅按真实标签权重加权样本 | `src/losses.py` | **难点 4** 长尾 |
| `mean_per_class_top1` / `macro_f1` / `..._support_ge5` 与 overall 并列上报 | `src/metrics.py` | **难点 4** 长尾 |
| 增广感知去重 + 分组感知切分（旋转/翻转变体哈希，双尺度判据） | `tools/dedup_check.py`, `src/data.py` | **难点 2 + 4 的交叉后果**：数据集用旋转副本扩充稀有类，直接制造验证集泄漏 |

---

## 3. 实验阶梯

每一级相对其父级**只改一个字段**；`analyze.py` 自动打印该字段 diff 与相对 L0 的 Δ。

| id | 父级 | 改动 | 验证假设 |
|---|---|---|---|
| L0 | — | ResNet50 @224 基础增广 | 地板线 |
| L0_naive_split | L0 | `group_aware_split: true→false` | **量化数据泄漏的贡献** |
| L1_320 / L1_448 | L0 | `img_size: 224→320/448` | 分辨率 vs 目标像素少 |
| L2 | L1_448 | `aug: basic→rs_rot` | 旋转增广 vs 朝向任意 |
| L3 | L2 | `head: gap→compact_bilinear` | 二阶特征编码 |
| L4 | L2 | `head: gap→cbam` | 弱监督定位 |
| L5 | L2 | `loss: ce→cb_ce` | 长尾（重点看 mean-per-class） |
| L6a / L6b | L2 | `model: resnet50→convnext_tiny / vit_base_patch16` | backbone 敏感性 |

---

## 4. 结果

见 `runs/_analysis/TABLE.md`（由 `tools/analyze.py` 自动生成）。当前表为 **seed 0
单次运行**。

### 4.0 报数口径：为什么不报 best epoch

本报告的所有数字是**收敛值 = 最后 10 个 epoch 的均值 ± epoch 间抖动**，不是验证集
上最好的那个 epoch。

原因是一个必须承认的方法学缺陷：本项目**没有独立测试集**，`train.py` 用 val 的
mean-per-class 选 checkpoint，又把同一个 val 上的分数当结果上报。在 60 个 epoch 上
取最大值，等于取一条噪声序列的极大值——**曲线抖得越厉害的那一档赢**，与模型好坏无关。
这不是理论担忧，实测的选择增益（`TABLE.md` 的 selection gap 表）：

| | L2 | L5 | L6a | L1_448 |
|---|---:|---:|---:|---:|
| MPC @best epoch | 97.46 | 97.49 | 97.49 | 92.81 |
| MPC 收敛值 | 94.97 | 97.47 | 97.43 | 92.24 |
| 选择增益 | **+2.49** | +0.02 | +0.06 | +0.57 |

L2 的 97.46 有 2.49 点来自 ep30 的一次尖峰（它在最后 10 个 epoch 只有
94.97 ± 0.36）。按 best-epoch 口径，L2/L5/L6a/L6b 全部并列在 97.49，阶梯什么也
分辨不出；换成收敛口径，三条结论发生**反转**，见 4.1/4.2/4.3 的粗体条目。

收敛口径仍不是正解，只是零成本的补救。**正确做法是三分切分**（train/val/test 按
组 60/20/20，val 选 epoch、test 报数），这需要全部重跑，列为待办。

### 4.1 基线、泄漏与分辨率

- `L0`：**overall 98.45±0.08 / 全类 MPC 89.20±0.60 / support≥5 MPC 97.04±0.16 /
  macro-F1 89.12±0.44**。overall 与全类 MPC 相差 9.25 点，直接显示 583x
  长尾下只报 overall 会掩盖尾类失败。
- `L0_naive_split` 相对 L0：overall **+0.48**、全类 MPC **+0.96**、
  support≥5 MPC **+1.68**、macro-F1 **+1.29** 点。泄漏一致地抬高全部指标，
  且对有充分支撑的类别影响最大。
  *（按 best-epoch 口径这里曾读成 overall **-0.16**、"泄漏不会简单抬高所有指标"，
  那是因为该 run 的 best 落在 ep7 的一次早期尖峰上。）*
- `L1_320` 相对 L0：overall **+0.72**、全类 MPC **+1.76** 点，增加有效输入像素有收益。
- **`L1_448` 相对 `L1_320`：overall +0.22、全类 MPC +1.28、support≥5 MPC +1.46 点。**
  448 明确优于 320，尤其在有充分支撑的类别上；代价是每 epoch 从 14.0 s 增至 26.0 s。
  *（旧结论"448 不如 320、主配方应优先 320"是错的：`L1_448` 的 best 落在 ep13，
  收敛后 overall 反而比该尖峰高 0.30 点。）*

### 4.2 模块消融

- `L2`（旋转增强）相对 `L1_448`：overall **+0.36**、全类 MPC **+2.73**、
  support≥5 MPC **+0.66**、macro-F1 **+2.65** 点。它是本阶梯中唯一明显改善了
  **有充分支撑类别**的模块，也最符合遥感朝向任意的先验。
- `L3`（紧凑双线性）相对 `L2`：overall **-0.72**、全类 MPC **-3.42**、
  support≥5 MPC **-1.62** 点，且每 epoch 更慢（31.1 s vs 26.0 s），明确不保留。
- `L4`（CBAM）相对 `L2`：overall **-0.10**、全类 MPC **-0.53** 点，且抖动最大
  （±1.23 vs L2 的 ±0.36）；没有证据支持额外注意力模块。
- **`L5`（CB-CE）相对 `L2`：全类 MPC +2.50 点（97.47±0.02 vs 94.97±0.36）、
  macro-F1 +2.61 点。** 差距远超两者的 epoch 间抖动，是本阶梯上除 rs_rot 外唯一
  站得住的模块收益。*（旧结论"仅 +0.03，可能是噪声"是 best-epoch 口径的假象。）*

`cb_ce` 曾发现并修复一个关键实现陷阱：直接把 `weight` 与 `label_smoothing=0.1`
同时传给 PyTorch `CrossEntropyLoss`，会把最高达 12.80 的稀有类权重施加到平滑目标的
所有类别项，首轮 top-1 仅 0.05%。当前实现先计算逐样本平滑 CE，再按真实标签对应权重
归一化加权；数值测试证明 label smoothing=0 时与标准加权 CE 等价（误差 1.19e-7）。

### 4.3 主干敏感性与尾类统计

**注意 `L6a`/`L6b` 用的是普通 CE（继承自 `L2`），不是 CB-CE。**

- **`L6a ConvNeXt-Tiny`：全类 MPC 97.43±0.00，相对同样用 CE 的 `L2`(ResNet50)
  +2.46 点。** 参数 23.59M→27.85M，每 epoch 反而更快（23.4 s vs 26.0 s）。
- **`L6b ViT-B/16`：97.46±0.01，同样相对 `L2` +2.49 点**，但 macro-F1 只有 96.55
  （比 L6a 低 0.74），参数 86.28M、每 epoch 50.4 s。召回打平而误报更多，成本最高。
- *（旧结论"L6 与 L5 实质并列、没有足够证据替换 ResNet50"是错的：换 backbone 的
  收益 ≈ +2.5 全类 MPC，与 CB-CE 同量级，且两者尚未组合验证。）*

**但尾部效应必须同时声明。** 验证支持仅 1 张的类别每多判对 1 张，全类 MPC 就跳
**1/42 = 2.38** 个百分点；支持 2 张的类别单张跳变为 **1.19** 点。上面那些 "+2.5 点"
落在 12 个 support<5 的类上，量级正好是一两张图。而在 30 个 support≥5 的类上，
`L2/L4/L5/L6a/L6b` 收敛值全部落在 **99.71–99.79%**，彼此差异 < 0.09 点，**完全打平**。

因此本项目当前能下的结论只有这几条：

1. **rs_rot 旋转增强有效**，是唯一改善了有充分支撑类别的模块（support≥5 MPC +0.66）；
2. **CB-CE 与更强 backbone 各带来约 +2.5 全类 MPC，但全部来自尾部类**——单种子
   且无测试集，这只能算候选信号，需 seeds 1/2 与三分切分复验；
3. **`L3` 紧凑双线性明确有害，`L4` CBAM 无收益**，这两条最稳；
4. 阶梯在 `L2` 之后已触及天花板（1880 张验证图上只错 2–5 张），继续在本切分上比较
   模块已无分辨力。

---

## 5. 复现

```bash
python tools/inspect_data.py  <root> --out runs/_data/<name>
python tools/dedup_check.py   <root> --out runs/_data/<name>
python tools/make_cache.py    <root> <cache> --max-short 512
python tools/run_ladder.py    configs/fgscr42/*.yaml --seeds 0
python tools/analyze.py       --runs runs --baseline L0
```

正式统计结论需补跑 `--seeds 1 2`；批量脚本会跳过 seed0，并从每个未完成任务的
`latest.pt` 精确恢复模型、优化器、学习率调度器和随机状态。

**FGSCR-42 没有官方 train/test 划分**（发布包只有 42 个类目录），本报告的切分是
自定义的分组感知 75/25。因此这些数字**不能与已发表的 FGSCR-42 结果直接比较**。

环境：Python 3.14.3 / torch 2.11.0+cu128 / timm 1.0.27 / RTX 5090 D。
`cudnn.deterministic = True`（未为速度放弃）。混合精度使用 **bfloat16**
（Blackwell 原生支持，无需 GradScaler）。

### 已知工程约束

- Windows 的 DataLoader 共享内存由 C: 分页文件（4 GB）支撑。8 workers × 448px ×
  并发 3 个任务会触发 `Couldn't open shared file mapping ... 1455`。
  处置：`data.workers` 降至 4，`run_ladder.py` 串行排队。分页文件未做改动
  （C: 仅剩 6.4 GB，且本机硬约束为 C: 不放占空间的东西）。
- 训练一律读 JPEG 缓存而非原始 BMP，否则 60 epoch × 8.4 GB 的 I/O 会主导耗时。
- 每个 epoch 原子写入 `latest.pt`，最佳模型单独写入 `best.pt`。前者用于严格续训，
  后者用于分析，避免中断后用“最佳权重 + 重置优化器”的非等价近似恢复。

### 待办与已知偏差

1. **无测试集**（§4.0）。三分切分需全部重跑，是唯一能让结论定稿的改动。
2. **`rs_rot` 的旋转填充参数已修正，但现有 `L2`–`L6b` 结果是修正前跑的。**
   原 `ROT_PAD = 1.15` 只覆盖 ±9.4°，实测 66.5% 的训练样本带黑角（最多占 5.3%
   像素）；现为 `cos30+sin30 = 1.366`，实测黑角归零。**`L2` 及其所有子级需重跑
   才能与当前代码一致。**
3. **`rs_rot` 同时改动了两件事**：旋转，以及"先放大再中心裁剪"引入的 1.366x zoom
   与裁边——而验证变换没有这个 zoom，存在训练/验证尺度失配。
   `L1_448 → L2` 的 +2.73 点里有多少来自旋转、多少来自 zoom，目前未拆分。
   建议增设只做 zoom 不做旋转的对照档。
4. `metrics.json` 的 `upsampled` 标志用全库短边 p95(=1112) 判定，`448 > 1112`
   恒假，该标志从未生效。分辨率是否超出原生像素应按图统计（§1.1 已有：224 档
   24.2%、448 档 43.8%）。
5. `weight_decay = 0.05` 施加到了 norm/bias 参数上；timm 的 ConvNeXt/ViT recipe
   会排除这两类。
6. `L6b` 用 `vit_base_patch16_224` 跑 448 输入，timm 会插值位置编码；该事实未记入
   `metrics.json`。改用 `vit_base_patch16_384` 更干净。
7. ~~OOM 回退分支重算了 `warmup`/`total` 但未按新 batch size 重算 `sched_state.step`，
   会造成学习率跳变。~~ **已修复**：`train.py` 在重建 loader 后把 `step` 回卷到当前
   epoch 在新迭代空间中的起点。本轮实验未触发 OOM，现有结果不受影响。
