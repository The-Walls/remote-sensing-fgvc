_base_: L2.yaml

# Only loss.name differs from L2. Domain difficulty #4: the train split is
# 583x imbalanced, so overall top-1 is dominated by head classes. Watch
# mean_per_class_top1, not overall.
exp_name: L5_cb_ce

loss:
  name: cb_ce
