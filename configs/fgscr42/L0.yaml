_base_: ../base.yaml

exp_name: L0

data:
  # JPEG cache (short side <= 512) built by tools/make_cache.py: 8831 MB -> 663 MB.
  # 512 is above every sweep point, so no experiment sees a degraded image.
  root: D:\rs-project\data\FGSCR-42-cache512
  dup_groups: D:\rs-project\fgvc\runs\_data\FGSCR-42\dup_groups.json
  img_size: 224
  aug: basic

model:
  name: resnet50
  head: gap

loss:
  name: ce
